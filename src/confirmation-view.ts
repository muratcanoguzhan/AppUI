export class ConfirmationView {
  routeConfig: any;
  constructor() {
  }

  activate(params, routeConfig, a) {
    this.routeConfig = routeConfig;
    if (params.id) {
    }
  }
  canDeactivate() {

  }
}
